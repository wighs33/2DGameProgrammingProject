1.

ㅈ,ㅗ,ㅎ,ㅕ,ㄴ,ㅅ,ㅣ,ㄱ 을 그리는 함수를 각각 만들어서 그것들을 조합해서 그렸습니다.

 

2. 첨부제출

 

3.

함수는 어떤 일을 처리하는 수식을 정의한 것입니다. 라이브러리, 모듈은 여러개의 함수로 이루어져 있습니다. 

함수의 형식은 다음과 같습니다.

 

def [함수 이름]([인수]):

[들여 쓰기] 실행할 내용1

[들여 쓰기] 실행할 내용2

 

함수를 정의하고 함수를 호출했을 때에 함수가 실행됩니다.

함수를 사용하면 여러 작업을 모아서 한번에 처리할 수 있습니다.

파이썬에서 함수는 리턴값을 여러 개 가질 수 있고 인자 타입에따라 연산기능이 결정됩니다.

 

4.

자기 자신을 재참조하는 방법으로 정의된 함수가 작업을 수행하는 방식입니다.

Python은 함수 깊이 제한이 기본 1000으로 되어 있어서 995회쯤 재귀를 하면 프로그램이 종료됩니다. 또한 재귀함수를 호출할 때 스택을 재사용하면서 메모리를 과도하게 사용하지 않도록 최적화하는 방법인 꼬리재귀를 지원하지 않습니다.

 

Python에서 활용 예

 

def hello(count):

    if count == 0:    # 종료 조건을 만듦. count가 0이면 다시 hello 함수를 호출하지 않고 끝냄

        return

    

    print('Hello, world!', count)

    

    count -= 1      # count를 1 감소시킨 뒤

    hello(count)    # 다시 hello에 넣음

 

hello(5)    # hello 함수 호출

 

5.

일부 작은 조각이 전체와 비슷한 기하학적 형태를 갖는 기하학적 구조를 프랙탈 구조라고 합니다.

프랙탈 구조는 자연물에서 뿐만 아니라 수학적 분석, 생태학적 계산, 위상 공간에 나타나는 운동모형 등 곳곳에서도 발견되어 자연이 가지는 기본적인 구조입니다.

 

프랙탈을 네 가지 생성 기법에 따라 분류할 수 있습니다.

시간매개형 프랙탈(Escape-time fractals, 궤도 프랙탈): 대개 복소평면 상에서, 각각의 점이 발산하는 속도를 색으로 나타낸 이미지. 망델브로 집합

반복함수계(Iterated function system): 기하학적 대체 규칙에 의해 만들어진 도형. 칸토어 집합, 시에르핀스키 삼각형과 시에르핀스키 카펫, 코흐 곡선, 페아노 곡선 등이 이에 해당한다.

기이한 끌개(Strange attractors): 주어진 사상이나 방정식의 해를 이용해 초기값을 반복적으로 변환한 것이며 혼돈 이론과 관계된다.

무작위적 프랙탈(Random fractals): 결정론적이지 않고 추측 통계학적으로 만들어진 것.

 

Python Turtle Graphics 의 가장 큰 장점은 프로그램이 수행하는 작업에 대한 즉각적인 시각적 피드백을 제공하고 반복된 작업을 쉽게 할 수 있다는 것입니다. 몇가지 재귀함수들로 멋진 프랙탈을 만들 수 있습니다.